<%= form_with(model: parametro, local: true) do |form| %>
  <% if parametro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(parametro.errors.count, "error") %> prohibited this parametro from being saved:</h2>

      <ul>
      <% parametro.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group row col-sm-4">
    <%= form.label :atributo %>
    <%= form.text_field :atributo, id: :parametro_atributo, class:"form-control" %>
  </div>

  <div class="field form-group row col-sm-4">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion, id: :parametro_descripcion, class:"form-control"  %>
  </div>

  <div class="field form-group row col-sm-4">
    <%= form.label :estadoParametro %>
    <%= form.text_field :estadoParametro, id: :parametro_estadoParametro, class:"form-control"  %>
  </div>

  <div class="actions">
    <%= form.submit "Guardar", :class=>"btn btn-secondary" %>
  </div>
<% end %>
